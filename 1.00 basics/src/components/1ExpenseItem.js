// a component in react is just a javascript function
//its better if the name of component fies give up a lil abput what they hold

function Expense() {
  return (
    <div>
      <p>Expense Item</p>
    </div>
  );
}
//this function need to be exported to be used we use
export default Expense;
//we will imporrt to app.js
